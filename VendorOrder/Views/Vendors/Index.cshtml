@{
  Layout = "_Layout";
}
  <body>
    @using VendorOrder.Models;

    <h1>Our registered partner vendors</h1>

    @if (@Model.Count == 0)
    {
      <h3>Nothing has been added yet!</h3>
    } 

    @foreach (Vendor vendor in Model)
    {
      <h3><a href='/vendors/@vendor.Id'>@vendor.Name</a></h3>
    }

    <p><a href='/'>Back home</a></p>
  </body>
</html>